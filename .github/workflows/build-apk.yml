name: Build APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.5'
          channel: 'stable'

      - name: Create missing platform folders (android/ios)
        run: flutter create --platforms=android --project-name=dialogue_app .

      - name: Fix minSdkVersion for flutter_tts
        run: |
          sed -i 's/minSdkVersion flutter.minSdkVersion/minSdkVersion 21/' android/app/build.gradle
          sed -i 's/minSdkVersion [0-9]\+/minSdkVersion 21/' android/app/build.gradle
          cat android/app/build.gradle | grep minSdkVersion

      - name: Install dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: dialogue-app-apk
          path: build/app/outputs/flutter-apk/app-release.apk
